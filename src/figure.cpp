#include "figure.h"

Coord Figure::Check(float x, float y, float z, Vector *r) {
  Vector some(0, 0, 0);
  Vector V(0,0,0);
  Coord coord{NAN,NAN,NAN, some, V};
  return coord;
}

Coord Figure::MinimalD(float x, float y, float z) {
  Vector some(0, 0, 0);
  Vector V(0,0,0);
  Coord coord{NAN,NAN,NAN, some, V};
  return coord;
}
